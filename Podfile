# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'PhotoGrid' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PhotoGrid
  pod 'SwiftLint'
end

target 'PhotoGridTests' do
  inherit! :search_paths
  pod 'SwiftyMocky','4.0.1'
end
